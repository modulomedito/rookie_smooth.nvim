# rookie_smooth.nvim

## Installation

Use lazy.nvim to install this plugin.

```lua
require("lazy").setup({
    {
        "rookie_smooth.nvim",
        config = function()
            require("rookie_smooth").setup()
        end,
    },
})
```
